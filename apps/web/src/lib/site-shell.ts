/** Header and full-width catalog pages share this content width. */
export const SITE_MAX_WIDTH_CLASS = 'max-w-6xl';

/** Horizontal gutters aligned with `SiteHeader`. */
export const SITE_GUTTER_CLASS = 'px-6';

export const SITE_SHELL_INNER_CLASS = `mx-auto w-full ${SITE_GUTTER_CLASS} ${SITE_MAX_WIDTH_CLASS}`;
