#!/usr/bin/env bash
# Convenience: use repo-local or PATH pnpm
ROOT="$(cd "$(dirname "$0")" && pwd)"
if [[ -x "$ROOT/node_modules/.bin/pnpm" ]]; then
  exec "$ROOT/node_modules/.bin/pnpm" "$@"
fi
exec pnpm "$@"
